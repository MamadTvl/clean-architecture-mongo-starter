import { Injectable, OnModuleInit } from '@nestjs/common';

@Injectable()
export class SeedService implements OnModuleInit {
  // some use case to insert from movie directory

  onModuleInit() {
    // throw new Error('Method not implemented.');
  }
}
